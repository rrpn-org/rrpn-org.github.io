import { type TestimonialItem } from "../types/configDataTypes";

/**
 * Testimonials are not currently used on the RRPN site.
 * If community or partner quotes are approved for use later, add them here
 * and render a testimonials component on the relevant page.
 */
export const testimonialData: TestimonialItem[] = [];

export default testimonialData;
